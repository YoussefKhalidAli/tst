apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: iti-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: iti-ca
  secretName: iti-ca-secret
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
