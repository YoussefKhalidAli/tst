apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: app-wildcard-cert
  namespace: default
spec:
  secretName: app-wildcard-tls
  issuerRef:
    name: iti-ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - "*.app.iti"
